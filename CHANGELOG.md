# 更新日志

## 1.0.0 — 2026-08-08

### 界面重构
- 参数设置由「7 个分类页签」改为**侧边栏分类导航**（基本设置 / 网络 / 音视频 / 后处理 / 直播 / 高级），每个分类内按语义分组展示
- **下载列表与参数设置拆分为两个独立页面**，各自占满可用空间，互不挤压
- 顶部输入区统一：程序 / ffmpeg / 链接 / 保存文件名各行 label 对齐、控件高度一致
- 并发限制与独立终端收纳进「基本设置」页的「运行控制」分组

### 新功能
- **深色主题完整适配**：自定义调色板 + 样式表覆盖日志、列表、菜单、滚动条等所有区域
- 命令预览改为**多行等宽**展示，长命令按宽度自动换行
- 日志长行自动换行，不再横向滚动
- 剪贴板自动获取链接时，底部状态栏显示**绿色提示**并同步写入日志区

### 移除
- **配置预设系统**（`config.ini` 已自动持久化整套设置，多套命名预设价值有限）
- **「生成命令」按钮**（命令预览本就随参数实时刷新，按钮冗余）

### 修复
- 「打开输出目录」改用 `explorer.exe` 直接打开，避免在部分系统上误开终端
- 程序 / ffmpeg 路径自动检测的绿色边框在浅色主题下背景与普通输入框不一致
- 路径检测到异常值后绿色边框残留
